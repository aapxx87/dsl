# 🧠 Креативный фреймворк: документация и структура

---

## 📍 Введение: что это за система

Это семантический фреймворк, описывающий условия, при которых возможна генерация инсайта (озарения) в когнитивной системе. Он моделирует:

- Влияние контекста (окружающих и внутренних факторов)
- Внутреннее состояние ума (attention, мышление, давление)
- Богатство и разнообразие накопленного опыта


Уровень абстракции:
Здесь не моделируются конкретные события, а задаются категории, которые объясняют:
- откуда взялся инсайт,
- какие условия критичны,
- как можно на это повлиять.

Цель — выразить **не просто логику**, а **структуру мышления**, пригодную для анализа, рефлексии, диагностики или симуляции.

---

## 🎯 Entry Point: точка входа в модель

Всё начинается с правила:

```
entry_point = {
  context: context_from_modifiers(modifiers),
  state: {
    mental_pressure: derive_state_from_context(mental_pressure, context),
    physical_state:  derive_state_from_context(physical_state, context),
    emotional_state: derive_state_from_context(emotional_state, context)
  },
  experience_bank: experience_bank_from_sources(experience_sources),
  evaluate: mapping insight
}
```

- `context` формируется на основе модификаторов
- `state` вычисляется через `derive_state_from_context`
- `experience_bank` собирается из источников
- `evaluate` — применяет центральное правило `mapping insight`

Ключевое: контекст формируется не вручную, а динамически на основе активных модификаторов. Затем через `derive_state_from_context` из него выводятся состояния.

---

## 🌲 Визуальная структура модели (от Mapping вниз)

Mapping инсайта задаёт условия, при которых когнитивная система способна сгенерировать новое понимание.

Требует:
- среды с низким шумом и возможностью блуждания  
- внутреннего состояния, допускающего ассоциативное мышление  
- достаточно разнообразного опыта

Результат: повышение вероятности появление "insight" — точки пересборки смысла или озарения.


```
  MAPPING: insight
  ├── requires:
  │   ├── environment
  │   │   ├── physical_context: [“nature”, “urban_park”]
  │   │   ├── spatial_quality: [“calm”, “safe”, “quiet”]
  │   │   └── activity:
  │   │         indoor:  [“open_monitoring_meditation”]
  │   │         outdoor: [“walk”, “sitting”, “wandering”]
  │   ├── internal_state
  │   │   ├── focus_mode: “diffuse”          ← fixed
  │   │   ├── thinking_pattern: “non_linear” ← fixed
  │   │   ├── mental_pressure                ← derived
  │   │   ├── physical_state                 ← derived
  │   │   └── emotional_state                ← derived
  │   └── experience_bank
  │       └── richness():
  │           ├── domain_count ≥ 5
  │           ├── overlapping == “low”
  │           └── associative_bridges == “present”
  │               └── experience_sources:
  │                   ├── literature
  │                   ├── hobbies
  │                   ├── personal_projects
  │                   └── social_circle
  ↓
  PRODUCES: [“insight”]
```

---

## 🧾 Параметры и уровни




### `param focus_mode`

Режим внимания:

- diffuse — рассеянное восприятие, позволяет улавливать слабые сигналы и неявные связи.
- narrow — сосредоточенность на конкретной задаче, хорошо для линейных решений.
- unfocused — хаотичное внимание без вектора, снижает продуктивность.

Важно различать diffuse и unfocused — первое продуктивно, второе — нет.

- `diffuse`: рассеянное восприятие, открытое к ассоциациям
- `narrow`: сосредоточенность на задаче
- `unfocused`: неконтролируемое внимание





### `param thinking_pattern`

Форма текущего мышления:
- `non_linear`: ассоциативное, метафорическое, не по шагам.
- `structured`: логичное, последовательное, используется в аналитике.
- `wandering`: случайное, без структуры, может быть источником озарений, но редко управляемо.


### `param mental_pressure`

Ментальное давление — это многослойное явление, объединяющее:

- субъективную загруженность (много мыслей в голове)
- эмоциональное напряжение (тревога, вина, спешка)
- соматическое состояние (усталость, недосып, истощение)

Даже при внешне "лёгком" расписании человек может ощущать high pressure — если на него давят внутренние установки или неразрешённые конфликты.

В модели креативности:

- High pressure → переключение на целевое, суженное внимание  
- Low pressure → активация default mode network → инсайты

Используется в internal_state, может быть вычислена из context через derive_state_from_context().

- `low`: ресурсное состояние, ясность
- `medium`: умеренное напряжение
- `high`: перегруз, тревожность




### `param physical_state`

- `stable`: хорошее самочувствие
- `fatigued`: накопленная усталость
- `exhausted`: физическое истощение




### `param emotional_state`

- `balanced`: эмоциональная устойчивость
- `irritated`: повышенная чувствительность
- `distressed`: напряжённое, дестабилизированное состояние


## ⚙️ Логика расчёта состояний


### `derive_state_from_context`

- Считывает активные влияния из `context`
- Применяет правила прироста / уменьшения score
- Конвертирует score в дискретный уровень (`map_score_to_level`)


### `context`

Контекст в этой модели — это не просто входной параметр, а динамически собираемое представление об активных влияющих факторах.

Он формируется из модификаторов, определённых в modifiers.positive и modifiers.negative.  
Сами модификаторы описывают, какие условия:
- снижают или усиливают давление  
- улучшают или ухудшают физическое и эмоциональное состояние  
- влияют на восприятие среды

В модели используется функция `context_from_modifiers(modifiers)`, которая на основе активных факторов из `modifiers` генерирует структуру `context.active`.

`derive_state_from_context`:
Эта функция вычисляет текущее состояние (например, mental_pressure, physical_state, emotional_state) на основе активных факторов в context.

Логика:

- из context.active собираются все активные влияния  
- каждый фактор сверяется с позитивными и негативными модификаторами  
- score увеличивается или уменьшается на основе влияния (±1)  
- score затем интерпретируется в уровень состояния (low, medium, high и т.д.) с использованием map_score_to_level()

Это создаёт прозрачный механизм: почему именно такое состояние возникло.





### `modifiers`

Словарь влияющих факторов:

- `reduces_mental_pressure`: снижает когнитивное напряжение
- `improves_physical_state`: улучшает физическое состояние
- `improves_emotional_state`: стабилизирует или улучшает настроение
- `disrupts_mental_pressure`: усиливает напряжение
- `worsens_physical_state`: ведёт к усталости
- `worsens_emotional_state`: вызывает эмоциональные перегрузки
- `affects_environment`: влияет на восприятие пространства

---


## 🧠 Опыт: experience_bank

Это абстракция, описывающая внутренний «резерв» разнообразных впечатлений, знаний, и ассоциаций.

Формируется из experience_sources.  
Богатый experience_bank должен:
- покрывать несколько семантически разных доменов  
- содержать мосты между этими доменами  
- быть достаточно свежим и насыщенным

Используется в качестве материала, из которого может быть построен инсайт.

Формируется из:

```
experience_sources = {
  literature:        ["sci-fi", "classics", "nonfiction"],
  personal_projects: ["creative_writing", "tool_design"],
  social_circle:     ["scientists", "artists"],
  hobbies:           ["birdwatching", "urban_exploration"]
}
```

Критерии насыщенности (функция `experience_bank_richnes`):

- ≥5 разных доменов
- низкое перекрытие
- наличие ассоциативных мостов



## 📚 Расширённые пояснения 

### `entry_point`

> Основной маршрут: context → state → experience_bank → mapping. Позволяет исследовать вопрос: «почему возник инсайт?» или «почему он невозможен в текущем состоянии?»

### `experience_bank`

> Внутренний «резерв» знаний и впечатлений. Богатство определяется по трём критериям:
- охват разных доменов
- низкое перекрытие
- наличие ассоциативных мостов

### `mental_pressure`

> Интегральный показатель нагруженности ума. Включает тревогу, переутомление, внутренний прессинг. Низкое давление коррелирует с инсайтами.

### `derive_state_from_context`

> Функция, вычисляющая уровни состояний по активным факторам контекста. Использует подсчёт score и сопоставление с порогами (`thresholds`).

### `experience_sources`

> Источники для формирования банка опыта: литература, личные проекты, круг общения, хобби. Разнообразие и насыщенность — ключ к инсайтам.

---

