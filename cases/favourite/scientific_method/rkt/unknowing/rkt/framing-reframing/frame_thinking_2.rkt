#lang racket

; Отличный вызов — это уже когнитивная модель с архитектурными ограничениями, которую мы хотим выразить в терминах поведения агента в Racket.

; Мы хотим смоделировать агента, который:
; 	•	🤔 исследует, сомневается, задаёт вопросы — но
; 	•	🧱 никогда не выходит за рамки определённой системы, например, религиозной, идеологической, методологической и т.д.

; ⸻

; 💡 Концепция:

; “Ограниченно-активное незнание” — это:
; 	•	внутренняя активность, реализованная через вопросы и сомнения;
; 	•	жёсткий фильтр, запрещающий выход за рамку;
; 	•	рамка, задающая допустимое пространство вопросов.


;; ─── 1. Определим рамку (допустимые темы) ────────────────────────────────────

(define рамка '(вера текст традиция)) ; допустимые сферы для поиска

;; ─── 2. Определим входящую ситуацию ─────────────────────────────────────────

(define тема 'вера) ; допустим, обсуждаем «вера»
(define интерес #t)
(define сомнение #t)
(define вопрос "Почему Бог допустил страдание?")

;; ─── 3. Функция проверки: находится ли вопрос ВНУТРИ рамки ──────────────────

(define (внутри-рамки? тема рамка)
  (member тема рамка)) ; возвращает #t, если тема разрешена

;; ─── 4. Агент с ограниченно-активным мышлением ──────────────────────────────

(define (ограниченно-активное-незнание? тема интерес сомнение рамка)
  (cond
    [(and интерес сомнение (внутри-рамки? тема рамка))
     'полная-активность-в-рамке]
    [(and интерес (внутри-рамки? тема рамка))
     'интерес-без-сомнения]
    [(and сомнение (внутри-рамки? тема рамка))
     'сомнение-без-интереса]
    [(and интерес сомнение)
     'активность-вне-рамки]
    [else
     'пассивность]))


(ограниченно-активное-незнание? 'вера #t #t '(вера текст))
;; => 'полная_активность_в_рамке

(ограниченно-активное-незнание? 'наука #t #t '(вера текст))
;; => 'активность_вне_рамки

(ограниченно-активное-незнание? 'вера #t #f '(вера текст))
;; => 'интерес_без_сомнения